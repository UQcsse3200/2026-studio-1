package com.csse3200.game.components.loot;

/**
 * this is the different types of items that can exist in the game.enum is used to create
 * pre-exisiting categories(any item belongs to one of these categories only)
 */
public enum ItemType {
  CONSUMABLE,
  WEAPON,
  CURRENCY
}
